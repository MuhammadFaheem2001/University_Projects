/* ==========================================================================
   config.js — kept only so every page's existing <script src="js/config.js">
   tag still has something to load.

   This site no longer talks to a backend at all. js/api.js is a
   self-contained client-side "backend" (accounts, sessions, predictions,
   saved patients) that lives entirely in this browser's localStorage.
   There's nothing here to point at a server, and nothing to configure.
   ========================================================================== */
